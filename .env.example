# ==================== JARBAS REMOTE BRIDGE CONFIG ====================

# Token de autenticação (OBRIGATÓRIO)
# Gere um token forte: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BRIDGE_TOKEN=seu_token_super_secreto_aqui_minimo_32_caracteres

# Porta do servidor (padrão: 8788)
BRIDGE_PORT=8788

# Ferramentas permitidas (separadas por vírgula)
# Opções: codex, claude, powershell, python, node, git, npm, etc.
ALLOWED_TOOLS=*

# Timeout de execução em milissegundos (padrão: 5 minutos)
COMMAND_TIMEOUT=300000

# Nível de log (debug, info, warn, error)
LOG_LEVEL=info

# Raízes de arquivo permitidas (separadas por vírgula, vazio = sem restrição)
# Restringe quais pastas o bridge pode acessar via /files/*
# Exemplo: ALLOWED_FILE_ROOTS=D:\projects,D:\jarbas_vida
ALLOWED_FILE_ROOTS=

# ==================== TAILSCALE (para container VPS) ====================
# Auth key do Tailscale (gere em https://login.tailscale.com/admin/settings/keys)
# Use uma key "Reusable" e "Ephemeral" para containers
# TAILSCALE_AUTHKEY=tskey-auth-xxxxxxxxxxxx
